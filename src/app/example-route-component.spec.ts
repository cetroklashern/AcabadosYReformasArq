import { ExampleRouteComponent } from './example-route-component';

describe('ExampleRouteComponent', () => {
  it('should create an instance', () => {
    expect(new ExampleRouteComponent()).toBeTruthy();
  });
});
